@model MyLibrary.ViewModels.BookSetViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Book</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @if (ViewBag.Message != null)
            {
                <div class="alert alert-warning">@ViewBag.Message</div>

            }
           

            <div class="form-group">
                <label asp-for="@Model.Libraries" class="control-label"></label>
                <input asp-for="@Model.Libraries" class="form-control" />
                <span asp-validation-for="@Model.Libraries" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Book.Name" class="control-label"></label>
                <input asp-for="Book.Name" class="form-control" />
                <span asp-validation-for="Book.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Book.Width" class="control-label"></label>
                <input asp-for="Book.Width" class="form-control" />
                <span asp-validation-for="Book.Width" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Book.Height" class="control-label"></label>
                <input asp-for="Book.Height" class="form-control" />
                <span asp-validation-for="Book.Height" class="text-danger"></span>
            </div>
            <div class="form-group">
                <select asp-for="LibraryId">
                    @foreach(var library in Model.Libraries)
                    {
                        <option value="@library.Id">@library.Genre</option>
                    })
                </select>
                
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
